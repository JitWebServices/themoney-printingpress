<footer class="text-white bg-[linear-gradient(135deg,#701b20_0%,#5a1417_55%,#701b20_100%)] relative">
  <!-- subtle top separator shadow -->
  <div class="absolute inset-x-0 top-0 h-px bg-white/10"></div>

  <div class="max-w-7xl mx-auto px-6 lg:px-8 py-12 md:py-14 grid gap-12 md:gap-10 md:grid-cols-[1.2fr_1fr_1.1fr] items-start">
    
    <!-- Brand -->
    <div>
      <h2 class="text-lg font-semibold tracking-tight mb-3 text-[#C6A041] victoria-font">
        The Money-Printing Press
      </h2>
      <p class="text-sm leading-relaxed text-neutral-200">
        Historical finance insights on how wealth was built, protected, and lost—and the principles that still apply.
      </p>
    </div>

    <!-- Links & Connect -->
    <div class="grid gap-8 sm:grid-cols-2 md:grid-cols-1">
      <div>
        <h3 class="text-base font-semibold mb-3 text-[#C6A041]">Explore</h3>
        <ul class="space-y-2 text-sm text-neutral-300">
          <li><a class="transition-colors hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#C6A041]/60 rounded-sm" href="/archive">Archive</a></li>
          <li><a class="transition-colors hover:text-white focus-visible:ring-2 focus-visible:ring-[#C6A041]/60 rounded-sm" href="/about">About</a></li>
          <li><a class="transition-colors hover:text-white focus-visible:ring-2 focus-visible:ring-[#C6A041]/60 rounded-sm" href="mailto:marcos@moneyprinting.press">Contact</a></li>
        </ul>
      </div>

      <div>
        <h3 class="text-base font-semibold mb-3 text-[#C6A041]">Connect</h3>
        <p class="text-sm text-neutral-300 leading-relaxed">
          Questions or collaboration ideas? Reach out—always open to thoughtful dialogue.
        </p>
      </div>
    </div>

    <!-- Newsletter -->
    <section role="region" aria-labelledby="footer-newsletter-heading" class="max-w-sm ml-auto md:ml-0">
      <div class="bg-[#F8F5EF] text-[#2B1A12] border border-[#E4DAC7] rounded-lg p-6 shadow-sm">
        <h3 id="footer-newsletter-heading" class="text-base font-semibold tracking-tight mb-2">
          Join the Newsletter
        </h3>
        <p class="text-xs leading-relaxed text-[#2B1A12]/85 mb-4">
          Weekly historical finance insights. No spam.
        </p>

        <form id="footer-newsletter-form" novalidate class="space-y-3">
          <div>
            <label for="footer-newsletter-email" class="sr-only">Email address</label>
            <input
              id="footer-newsletter-email"
              type="email"
              required
              placeholder="Enter your email…"
              aria-describedby="footer-newsletter-error"
              class="w-full rounded-sm border border-[#D8CCB9] bg-white px-3 py-2 text-sm focus:outline-none focus:border-[#B59030] focus:ring-2 focus:ring-[#B59030]/40 transition"
            />
            <p id="footer-newsletter-error" class="mt-1 text-xs text-[#B3403B] hidden"></p>
          </div>
          <button type="submit"
            class="w-full bg-[#B59030] text-[#2B1A12] text-sm font-medium py-2.5 rounded-sm hover:bg-[#C6A041] active:bg-[#A07D27] focus:outline-none focus:ring-2 focus:ring-[#B59030]/50 transition">
            Subscribe
          </button>
        </form>

        <p id="footer-newsletter-success" class="mt-3 text-xs font-medium text-[#2B1A12] hidden">
          Check your inbox for confirmation.
        </p>
      </div>
    </section>

  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-white/15">
    <div class="max-w-7xl mx-auto px-6 lg:px-8 py-5 text-center text-xs md:text-sm text-neutral-300 flex flex-col sm:flex-row items-center justify-center gap-2">
      <span>&copy; <span id="year"></span> The Money-Printing Press.</span>
      <span class="hidden sm:inline text-neutral-500">|</span>
      <span>All rights reserved.</span>
      <span class="hidden sm:inline text-neutral-500">|</span>
      <span class="text-[#C6A041]">Marcos Vega</span>
    </div>
  </div>
</footer>

<script>
  document.getElementById('year').textContent = new Date().getFullYear();

  const form = document.getElementById('footer-newsletter-form');
  if (form) {
    const email = document.getElementById('footer-newsletter-email');
    const err  = document.getElementById('footer-newsletter-error');
    const success = document.getElementById('footer-newsletter-success');

    form.addEventListener('submit', e => {
      e.preventDefault();
      err.classList.add('hidden');
      err.textContent = '';
      const val = email.value.trim();
      const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

      if (!val) {
        showError('Email required.');
      } else if (!re.test(val)) {
        showError('Enter a valid email.');
      } else {
        form.classList.add('hidden');
        success.classList.remove('hidden');
      }

      function showError(msg){
        err.textContent = msg;
        err.classList.remove('hidden');
        email.focus();
      }
    });
  }
</script>

<style>
  .victoria-font { font-family: 'Victoria','Times New Roman',serif; }
  .sr-only {
    position:absolute; width:1px; height:1px; padding:0; margin:-1px;
    overflow:hidden; clip:rect(0 0 0 0); white-space:nowrap; border:0;
  }
</style>